sudo: required
services:
  - docker
language: bash

script:
  # prepare qemu
  - docker run --rm --privileged multiarch/qemu-user-static:register --reset
  # build alpine
  - chmod +x ./bootstrap.sh
  - docker run --rm -v ${TRAVIS_BUILD_DIR}:/host/ multiarch/alpine:armhf-edge /host/bootstrap.sh
    
after_success:
  - echo "Done!"
